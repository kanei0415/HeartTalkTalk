import Foundation
import SwiftUI

extension Image {
    enum Icon: String {
        case email = "icEmail"
        case password = "icPassword"
        case typo = "icTypo"
        case logo = "icLogo"
        case google = "icGoogle"
        case sender = "icSender"
        case send = "icSend"
    }
}
